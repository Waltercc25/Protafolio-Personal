/**
 * @deprecated Usa `@/lib/content` — capa MDX unificada.
 * Este archivo se mantiene vacío; los proyectos viven en content/projects/*.mdx
 */
export { getAllProjects as projects, getProjectBySlug, getFeaturedProjects } from "@/lib/content";
